<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{post.name}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"
        integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>

<body>
   {{> header}}
    <br>
    <div class="container mt-5">
        <div class="row justify-content-center">
        
            <div class="col-md-8">
                <div class="thumbnail">
                    <img class="img-fluid" src="{{post.image}}">
                    <div class="caption-full">
                        <h1 class="mt-3 fw-bolder"><a>{{post.name}}</a></h1>
                        <p>{{{post.description}}}</p>
                        <p>
                            <em>Submitted By {{post.author.username}}</em>
                        </p>
                        {{#if currentUser}}
                        <a class="btn btn-warning m-2" href="/posts/{{post._id}}/edit">Edit</a>
                        <form id="delete-form" action="/posts/{{post._id}}?_method=DELETE" method="POST">
                            <button class="btn btn-sm btn-danger m-2">Delete</button>
                        </form>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{> footer}}
</body>

</html>